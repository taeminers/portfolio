import { style } from "@vanilla-extract/css";

export const container = style({
  width: "100%",
  padding: "0 60px",
  background: "#333",
});

export const line = style({
  width: "100%",
  height: "1px",
});
