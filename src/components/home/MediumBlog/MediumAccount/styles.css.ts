import { style } from "@vanilla-extract/css";

export const container = style({
  cursor: "pointer",
});

export const avatar = style({
  borderRadius: "100px",
  width: "50px",
  height: "50px",
});
